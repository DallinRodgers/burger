<li>
	{{burger_name}}

	<button data-id="{{id}}" data-devoured="{{devoured}}" class="change-devoured {{#if devoured}}d-none{{/if}}">
		{{#if devoured}}SLEEP TIME!{{else}}Devour
		It!{{/if}} </button> </li>